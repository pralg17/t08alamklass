import java.util.*;

public abstract class Shapes{
	
	public double shapeArea(){return 0;} //Returning 0 because this is just a generic "Shape"
	public double shapePerimeter(){return 0;} //Returning 0 because this is just a generic "Shape"
	
}