class Train {
  String name;
  String builder;
  double maxSpeed;
  int designYear;

  public Train(String name, String builder, double maxSpeed, int designYear) {
    this.name = name;
    this.builder = builder;
    this.maxSpeed = maxSpeed;
    this.designYear = designYear;
  }
}
